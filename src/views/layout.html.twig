<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Coloc</title>
        <link href="/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" media="screen" />
        <link href="/lib/Font-Awesome/css/font-awesome.min.css" rel="stylesheet" media="screen" />
        <link href="/css/main.css" rel="stylesheet" media="screen" />
    </head>
    <body>
        <div class="container">
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="/">Coloc</a>

                <div class="navbar-collapse collapse d-flex justify-content-start">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <ul class="navbar-nav">
                            <li class="nav-item"><a class="nav-link" href="/expenses">Achats</a></li>
                            <li class="nav-item"><a class="nav-link" href="/payments">Remboursements</a></li>
                        </ul>
                        <ul class="navbar-nav ml-auto">
                            <li class="nav-item"><a class="nav-link" href="/logout">Déconnexion</a></li>
                        </ul>
                    {% endif %}
                </div>
            </nav>

            {% for type, messages in app.flashes() %}
                <div class="alert alert-{{ type }}" role="alert">
                    {% for message in messages %}
                        {{ message }}<br />
                    {% endfor %}
                </div>
            {% endfor %}

            {% block content %}{% endblock %}
        </div>
    </body>
</html>
